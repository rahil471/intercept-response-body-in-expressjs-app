# intercept-response-body-in-expressjs-app
An express middleware to log response body, intercept response body or to modify response body, in an NodeJS express app.

`git clone ` </br>
`npm install` </br>
`node index.js`
